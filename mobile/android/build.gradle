// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
  repositories {
    google()
    mavenCentral()
  }
  dependencies {
    classpath('com.android.tools.build:gradle')
    classpath('com.facebook.react:react-native-gradle-plugin')
    classpath('org.jetbrains.kotlin:kotlin-gradle-plugin')
  }
}

allprojects {
  repositories {
    google()
    mavenCentral()
    maven { url 'https://www.jitpack.io' }
  }
}

apply plugin: "expo-root-project"
apply plugin: "com.facebook.react.rootproject"

// Desliga as tasks lintVital* em todos os módulos.
// Esse lint regularmente trava builds Expo em Windows (expo-modules-core, expo-updates etc.).
// O APK funciona sem isso — pra rodar lint manualmente: gradlew lint
gradle.taskGraph.whenReady { graph ->
  graph.allTasks.each { task ->
    if (task.name.startsWith("lintVital")) {
      task.enabled = false
    }
  }
}
